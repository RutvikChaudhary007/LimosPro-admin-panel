// Helper function to generate consistent page titles
export const generatePageTitle = (pageTitle: string): string => {
  // Creates SEO-friendly titles with brand name at the end
  return `${pageTitle} | LimosPro`;
};
